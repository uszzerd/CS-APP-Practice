goto version
= 
사람이 직접 C로 번역하는 과정, 어셈블리 ↔ C 사이의 사고 훈련

if else, for, while, switch

if else :
    t = test-expr;
    if (!t)
        goto fales;
    then-statement
    goto-done;
false:
    else-statement;
done:

 
Do-While :
done    
    body-statement
    while(test-expr);

loop:
    body-statement
    t = (test-expr);
    if (t) 
        goto loop:


while :
while(test-expr)
    body-statement

    goto test;
loop:
    body-statement
test:
    t = test-expr;
    if(t)
        goto loop;


for :
for(init-expr; test-expr; update-expr)
    body-statement 

for로 생성된 문장의 최적화는 while, do-while 로 나뉘어 코드를 생성한다

init-expr
while (test-expr)
{
    body-statement
    update-expr;
}
=>
    init-expr;
    goto test;
loop:
    body-statement
    update-expr;
test:
    t = test-expr;
    if (t)
        goto loop;
,

    init-expr;
    t = test-expr;
    if (!t)
        goto done
loop:
    body-statement
    update-expr;
    t = test-expr;
    if (t)
        goto loop;
done:


switch : 

goto label;

label:
    statement;

end:
